

## Plan: Video de fondo más claro y vibrante

### Cambios

1. **Reducir la opacidad del overlay oscuro** en `src/pages/Landing.tsx`: cambiar `bg-black/60` a `bg-black/40` para dejar pasar más luz del video y que se vea más claro y vibrante.

2. **Buscar y reemplazar el video** (`public/videos/hero-background.mp4`) por uno más luminoso con temática de viajes, amigos, familia y diversión — colores cálidos, escenas alegres, paisajes bonitos.

### Detalle técnico

- Solo se modifica una clase CSS en el overlay del hero en `Landing.tsx`.
- Se reemplaza el archivo de video en `public/videos/`.

