

## Plan: Remove navbar logo and access button

Remove the entire navbar from `src/pages/Landing.tsx` (lines ~56-64) — the sticky nav containing the YORMIT logo in black and the "Acceder" button.

**File**: `src/pages/Landing.tsx`
- Delete the `<nav>` block entirely (the sticky top navbar with the logo and "Acceder" button)

