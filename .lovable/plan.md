

## Aumentar tipografía global de YORMIT

El font-size base actual ya es `17px` (establecido en el cambio anterior). Para aumentarlo un poco más, subiré a **18px** — otro ~6% de incremento. Mismo enfoque seguro: solo se toca una línea en `src/index.css`.

### Cambio

| Fichero | Línea | Antes | Después |
|---------|-------|-------|---------|
| `src/index.css` | `font-size: 17px` | `17px` | `18px` |

No se toca ningún otro fichero ni funcionalidad.

